{
  "tasks": {
    "start": "deno run --unstable-cron --env --allow-net --allow-env main.ts",
    "dev": "deno run --unstable-cron --env --watch --allow-net --allow-env main.ts",
    "check": "deno check main.ts src/**/*.ts",
    "test": "deno test --allow-env",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": false,
    "proseWrap": "always"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
